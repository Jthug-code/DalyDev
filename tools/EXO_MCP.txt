exo cluster (MCP) — Cursor configuration
========================================

Master Tailscale IP: 100.88.30.110

Canonical repo: https://github.com/Jthug-code/exo-mcp
Interim branch:   git clone -b exo-mcp-standalone https://github.com/Jthug-code/DalyDev.git exo-mcp

On the MASTER machine (Cursor + exo on same box):
   export EXO_BASE_URL=http://127.0.0.1:52415
   Use cursor-mcp.master-local.json

On another tailnet machine:
   export EXO_BASE_URL=http://100.88.30.110:52415
   Use cursor-mcp.example.json

Then: uv sync, merge MCP JSON, reload Cursor.
